[package]
name = "full"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0-rc", features = ["tls"] }
rocket_dyn_templates = { version = "0.1.0-rc.1", features = ["handlebars"] }
serde = "1.0"
